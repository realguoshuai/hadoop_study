奇葩的bug:
调试kafka消费者程序  修改后发现 一直有错误数据 然后就一直在改代码逻辑  发现仍然是有脏数据(3000w/天数据量) 开始以为是消费者逻辑太复杂,没消费完(一直优化)
一直找不到原因  偶然发现:本地IDE程序关闭运行后 hbase表中还是一直有数据增加  
查看任务管理器 一个java.exe的CPU一直在跳   关掉IDE 异常消失 
原因是没关干净(程序开太多,导致IDEA显示关掉了,后台还在跑)
